<main>
    {{!-- Formulário--}}
    <div class="container">
        <div class="row">
            <div class="col-6 offset-4">
                <form action="/editar" method="post">
                    <input type="number" name="id"               value="{{tarefa.id}}" readonly>
                    <input type="text"   name="nome"             value="{{tarefa.nome}}"        class="form-control" placeholder="Nome da Tarefa">
                    <input type="number" name="custo"            value="{{tarefa.custo}}"       class="form-control" placeholder="Custo da Tarefa">
                    <input type="date"   name="data_limite"      value="{{tarefa.data_limite}}" class="form-control" aria-label="Data Limite da Tarefa">
                    <input type="submit" class="btn btn-primary" value="Alterar">
                    <a href="/" class="btn btn-secondary">Cancelar</a>
                </form>

            </div>
        </div>
    </div>

    {{!--Listagem das Tarefas--}}
    <div class="container">
        <div class="row">
            {{#each tarefas}}
                <div class="col-12">
                    <div class="card">
                        <div class="textos">
                            <p>{{nome}}</p>
                            <p>R${{custo}}</p>
                            <p>{{data_limite}}</p>
                        </div>
                        <a href="/formularioEditar/{{id}}" class="btn btn-warning">Alterar</a>
                        <a href="/remover/{{id}}" class="btn btn-danger">Remover</a>

                    </div>
                </div>
            {{/each}}
            
        </div>

    </div>
</main>